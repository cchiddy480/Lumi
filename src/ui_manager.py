class UIManager:
    def __init__(self):
        # Initialize the list to store UI components
        self.components = []

    def add_component(self, component):
        # Add a new component to the UI manager
        self.components.append(component)

